#include "main.h"

/**
 * add - contains the command to be executed
 * @x: character to be executed
 * @y: character to be executed
 * Return: return result
 */

int add(int x, int y)
{
	result = x + y;
	return (result);
}

