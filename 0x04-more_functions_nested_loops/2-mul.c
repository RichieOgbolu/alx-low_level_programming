#include "main.h"

/**
 * mul - contains the command to be executed
 * @a: character to be executed
 * @b: character to be executed
 * Return: return the result of the two integers
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
